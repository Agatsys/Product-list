import React from "react";
import './length.scss'


const Length = (props) => {
    return (
        <div className='length__DetailPageWrapper'>
            {props.length}
        </div>
    )
}

export default Length;